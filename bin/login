#!/bin/bash

case $1 in
  funnel)
    ssh -i $SSH_IDENTITY_FILE root@161.35.130.103
    ;;

  controller)
    ssh -i $SSH_IDENTITY_FILE root@192.168.1.57
    ;;

  *)
    echo -n "Machine name not recognized"
    ;;
esac
